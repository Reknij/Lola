export interface AppConfig {
    fetch_mode: string,
    expired_days: number,
    auto_select_lane: boolean,
    mini_mode: boolean,
    cache_path: string,
    config_path: string,
    data_path: string,
}